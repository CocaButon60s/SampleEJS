{
  "name": "sampleejs",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "npm run build && nodemon src/server/main.js -w src/server",
    "start": "npm run build && cross-env NODE_ENV=production node src/server/main.js",
    "build": "vite build"
  },
  "devDependencies": {
    "nodemon": "^3.0.1",
    "vite": "^5.0.2"
  },
  "dependencies": {
    "bootstrap": "^5.3.7",
    "cross-env": "^7.0.3",
    "ejs": "^3.1.10",
    "express": "^4.18.2",
    "vite-express": "*"
  }
}
